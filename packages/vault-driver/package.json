{
  "name": "@medi-hive/vault-driver",
  "version": "0.1.0",
  "description": "Profile-agnostic vault interface. Implementations: SolanaDriver (on-chain) and LocalDriver (Postgres + hash-chained audit).",
  "license": "AGPL-3.0-or-later",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "lint": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "@types/node": "^20.0.0"
  }
}
